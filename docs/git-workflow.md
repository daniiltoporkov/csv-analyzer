# Git Workflow

Мы используем Git Flow:
- `master` – стабильные релизы
- `develop` – интеграция всех изменений
- `feature/*` – новая функциональность
- `hotfix/*` – исправления для релизов
- `release/*` – подготовка к релизу

Все Pull Request проходят ревью и CI перед слиянием.