# Архитектура проекта

Проект состоит из модулей:
- `reader` – загрузка CSV и представление тикета
- `stats` – агрегация (подсчёт, среднее, доля)
- `cli` – интерфейс командной строки

Зависимости: только стандартная библиотека Python, `pytest` для тестов.